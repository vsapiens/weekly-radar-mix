## Summary

Brief description of what this PR changes and why.

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Documentation update
- [ ] Refactor (no behavior change)
- [ ] Dependency update
- [ ] CI / workflow change

## How to test

```bash
# Commands to verify the change works
python weekly_mix_review.py --dry-run --verbose
```

## Checklist

- [ ] Code runs locally without errors (`make run` or `python weekly_mix_review.py --dry-run`)
- [ ] Code is formatted (`make format`)
- [ ] Code passes linting (`make lint`)
- [ ] `CHANGELOG.md` updated under `[Unreleased]`
- [ ] README updated if user-facing behaviour changed
- [ ] No credentials, `.env`, or `.cache` files included
